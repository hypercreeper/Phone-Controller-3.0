<Page
    x:Class="Phone_Controller_3._0.Views.CommandLinePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ProgressBar x:Name="cmdLineProgressIndicator" Grid.ColumnSpan="2" Visibility="Collapsed" Grid.Row="0" Grid.Column="0" IsIndeterminate="True" Margin="0,0,0,0" />
        <AutoSuggestBox x:Name="cmdLineInput" Grid.Row="1" Grid.Column="0"  TextChanged="AutoSuggestBox_TextChanged" SuggestionChosen="AutoSuggestBox_SuggestionChosen" KeyUp="cmdLineInput_KeyUp" />
        <Button x:Name="sendCommand" Grid.Row="1" Grid.Column="1" Click="sendCommand_Click">
            <FontIcon Glyph="&#xE724;"/>
        </Button>

        <TextBox x:Name="cmdLineOutput" TextWrapping="Wrap" Grid.ColumnSpan="2" IsReadOnly="True" Grid.Row="2" Grid.Column="0" AcceptsReturn="True" Margin="0,20,0,20" />
    </Grid>
</Page>
